name: Publish kit

# Publish the mixin kit to GitHub Container Registry as an OCI artifact whenever
# the kit changes on main (as :latest) and on version tags (as :vX.Y.Z). Modeled
# on ajeetraina/sbx-kits-* CI, retargeted to ghcr.io (no extra secret — the
# built-in GITHUB_TOKEN can push packages).
on:
  push:
    branches: [main]
    paths:
      - kit/**
      - sandbox-kit/**
      - scripts/push-kit.sh
      - .github/workflows/publish-kit.yaml
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      sbx_release:
        description: "docker/sbx-releases tag to install (must support kit-spec v2)"
        required: false
        default: "latest"
      tag:
        description: "OCI tag to publish"
        required: false
        default: "latest"

permissions:
  contents: read
  packages: write

jobs:
  publish-kit:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Install sbx
        env:
          GH_TOKEN: ${{ github.token }}
          SBX_RELEASE: ${{ inputs.sbx_release || 'latest' }}
        run: |
          set -euo pipefail
          # NOTE: adjust the asset name if a future sbx release changes it.
          asset='DockerSandboxes-linux-amd64-ubuntu2604.deb'
          if [ "$SBX_RELEASE" = "latest" ]; then
            gh release download --repo docker/sbx-releases --pattern "$asset" --output /tmp/sbx.deb
          else
            gh release download "$SBX_RELEASE" --repo docker/sbx-releases --pattern "$asset" --output /tmp/sbx.deb
          fi
          sudo apt-get update
          sudo apt-get install -y /tmp/sbx.deb
          sbx version

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push kits (mixin + sandbox)
        env:
          REGISTRY: ghcr.io/${{ github.repository_owner }}
        run: |
          set -euo pipefail
          TAG="${{ inputs.tag || 'latest' }}" ./scripts/push-kit.sh
          KIT_DIR=sandbox-kit KIT_NAME=infrastructure-sandbox-kit \
            TAG="${{ inputs.tag || 'latest' }}" ./scripts/push-kit.sh
          if [ "${{ github.ref_type }}" = "tag" ]; then
            TAG="${{ github.ref_name }}" ./scripts/push-kit.sh
            KIT_DIR=sandbox-kit KIT_NAME=infrastructure-sandbox-kit \
              TAG="${{ github.ref_name }}" ./scripts/push-kit.sh
          fi
